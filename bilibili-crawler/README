<h>基于node.js的小爬虫，抓取某一页面视频相关信息</h>
使用方法：修改crawler.js中url变量为对应链接地址  切换到对应目录，安装好相应依赖后，npm start即可，在目录下即可找到对应番号的文本文件
主要实现思路，利用cheerio根据选择器获取html文档上的视频基本信息，用正则表达式在脚本中捕获aid和cid，并调用相关接口，进一步获取视频详细信息
遇到的几个坑：1.主页面是经过gzip压缩过的，不能直接获取，option中需加入gzip:true
2.详细信息获取的是类似xml格式的文本，这里在最外面又嵌套了一层标签，然后用了xml2js将其整体转为js对象
3.也尝试着抓取了响应弹幕信息不过直接变成了乱码，尝试多种编码方式包括iconv等模块均未解决，不知道是不是有什么反爬虫机制？留着慢慢研究了
